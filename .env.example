# Vibe Scaffold Configuration
# All variables use the VIBE_ prefix.
# Copy this file to .env and adjust as needed.

# Task directories (relative to workspace)
# VIBE_TASK_DIR=tasks
# VIBE_DONE_DIR=tasks/done
# VIBE_FAIL_DIR=tasks/failed

# Execution timeout in seconds (default: 600)
# VIBE_TIMEOUT=600

# Max retry count per task (default: 2)
# VIBE_MAX_RETRIES=2

# Number of parallel workers (default: 1)
# VIBE_MAX_WORKERS=1

# Workspace / target project root (default: .)
# VIBE_WORKSPACE=.

# Logging level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# VIBE_LOG_LEVEL=INFO

# Log file path (empty = stdout only)
# VIBE_LOG_FILE=

# Use git worktree isolation for multi-worker (default: true)
# VIBE_USE_WORKTREE=true

# Plan mode: generate plan before execution (default: false)
# VIBE_PLAN_MODE=false

# Auto-approve plans in plan mode (default: true)
# VIBE_PLAN_AUTO_APPROVE=true

# Docker isolation (default: false)
# VIBE_USE_DOCKER=false

# Docker image name (default: auto-claude-code)
# VIBE_DOCKER_IMAGE=auto-claude-code

# Extra docker run arguments
# VIBE_DOCKER_EXTRA_ARGS=
